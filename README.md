# מבחן המינוף — סימולטור מונטה קרלו ממונף

יישום אינטראקטיבי לניתוח אלפי עתידים אפשריים של השקעה במדד S&P 500 באמצעות מינוף יומי. הכלי מדגיש את הפער בין התוצאה הממוצעת לתוצאה החציונית, את שיעור המחיקה המלאה ואת השווי הסופי הממוצע בזנבות הגרוע והחיובי.

> **הבהרה:** זהו כלי מחקרי וחינוכי בלבד. התוצאות אינן תחזית ואינן מהוות ייעוץ השקעות.

## יכולות עיקריות

- השוואה ישירה בין רמות מינוף מותאמות אישית (`0.1×` עד `20×`) על אותם מסלולי שוק.
- תשואות יומיות מהתפלגות Student-t עם עובי זנבות ניתן לשליטה.
- רצפת שווי טבעית של אפס בלבד: אין סף מלאכותי שמוחק תיק שעדיין נותר בו ערך.
- חישוב אלפי מסלולים ב־Web Worker, עם חיווי התקדמות וממשק שאינו נחסם.
- חישוב מחדש באופן אוטומטי לאחר שינוי פרמטר, עם השהיה קצרה שמונעת עומס בזמן גרירה.
- גרף מסלולים לוגריתמי עם חציון, ממוצע ורצועות אחוזונים.
- היסטוגרמת שווי סופי ומדדי חציון, ממוצע, שיעור מחיקה מלאה, CVaR וממוצע זנב חיובי.
- תרחישי שוק מוכנים ומחולל אקראי ניתן לשחזור באמצעות seed.
- ממשק כהה, מלא בעברית ובכיוון RTL.

## דרישות

- Node.js בגרסה 20 ומעלה
- npm בגרסה 10 ומעלה

## התקנה והרצה

```bash
npm install
npm run dev
```

לאחר ההרצה, Vite יציג כתובת מקומית לפתיחת היישום בדפדפן.

## פקודות שימושיות

```bash
# הרצת בדיקות יחידה
npm test

# בדיקת איכות קוד
npm run lint

# יצירת גרסת ייצור
npm run build

# תצוגה מקומית של גרסת הייצור
npm run preview

# שחזור כיול זנב S&P 500 המתועד (דורש רשת)
npm run calibrate:sp500-tail

# אימות דטרמיניסטי של האתר החי וה־assets
npm run verify:live
```

## תיעוד הפרויקט

- [חוזה המוצר](docs/product-spec.md)
- [המודל המתמטי](docs/model.md)
- [מקורות וכיול](docs/sources.md)
- [החלטות ADR](docs/decisions/README.md)
- [נוהל עבודה ושחרור](docs/operations.md)
- [מצב נוכחי](docs/status.md)
- [הפעלת Codex](docs/codex-bootstrap.md)
- [יומן מימוש היסטורי](docs/implementation-log.md)

## המודל המתמטי

מספר ימי המסחר בשנה (`D`) ניתן לעריכה; ברירת המחדל היא 252. התשואה היומית של המדד מחושבת כך:

```text
r = μ / D + σ / √D × T(df) × √((df - 2) / df)
```

הגורם האחרון מנרמל את שונות התפלגות Student-t. לכן הממשק דורש `df > 2` ומאפשר טווח `2.01–100`, שבו קיימת שונות סופית. לכל מסלול שוק מוחלות כל רמות המינוף שנבחרו, כדי שההשוואה ביניהן לא תושפע מרצפים אקראיים שונים.

תשואת התיק הממונף בכל יום היא:

```text
V(t+1) = V(t) × max(0, 1 + L × r)
```

כאשר התשואה היומית הממונפת מוחקת את מלוא השווי, התיק מגיע לאפס ונשאר שם. ירידה מתחת לאחוז שרירותי אינה גורמת למחיקה.

## ארכיטקטורה

- `src/simulation/engine.ts` — לולאת הסימולציה והפקת התוצאות.
- `src/simulation/random.ts` — מחולל אקראי עם seed ודגימה מ־Student-t.
- `src/simulation/statistics.ts` — אחוזונים, CVaR והיסטוגרמה.
- `src/simulation/simulation.worker.ts` — הרצת החישוב מחוץ לתהליך הממשק.
- `src/components/Charts.tsx` — גרפי Canvas חסכוניים בתלויות.
- `src/App.tsx` — בקרי המשתמש, תרחישים ותצוגת המדדים.

## פריסה

היישום סטטי לחלוטין. הפקודה `npm run build` יוצרת את התיקייה `dist`, שאותה ניתן לפרוס ב־GitHub Pages, ב־Vercel או בכל שירות אירוח סטטי. הנתיב היחסי ב־Vite כבר מותאם לפריסה תחת נתיב משנה.
